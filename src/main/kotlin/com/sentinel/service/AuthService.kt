package com.sentinel.service

// This service has been superseded by UserService.kt
// Keeping file as placeholder to avoid potential build errors during refactor transition,
// but the class definition is removed to ensure no duplicate beans.
